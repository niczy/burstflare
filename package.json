{
  "name": "burstflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "ci": "npm run lint && npm run build && npm test",
    "dev": "node apps/edge/src/dev-server.js",
    "lint": "node scripts/lint.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
